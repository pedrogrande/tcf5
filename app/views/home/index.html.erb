<%= render 'header' %>

<div id="main">

	<%= render 'hero' %>

	<%= render 'about' %>

	<%= render 'course' %>
	
	<%= render 'schedule' %>
	
	<%= render 'team' %>

	<%= render 'faq' %>
	
	<%= render 'numbers' %>
	
	<%= render 'application_form' %>
	
	<%= render 'footer' %>
	
	<%= render 'enquiry_form' %>


<script>
$(document).ready(function() {
    $('#fullpage').fullpage({
        verticalCentered: false,
        resize : false,
        // slidesColor : ['#ccc', '#fff'],
        anchors:['home', 'coder-factory', 'course', 'schedule', 'team', 'faq', 'numbers', 'apply', 'contact'],
        scrollingSpeed: 1700,
        easing: 'easeOutSine',
        touchSensitivity: 6,
        css3 : true,
        fixedElements : '.navbar',
        scrollOverflow : false,
        paddingTop: 0
    });
});
</script>

<script>
$(document).ready(function() {
	$(".various").fancybox({
		maxWidth	: 800,
		maxHeight	: 600,
		fitToView	: false,
		width		: '70%',
		height		: '70%',
		autoSize	: true,
		closeClick	: false,
		openEffect	: 'none',
		closeEffect	: 'none',
		padding : 0,
		openEffect : 'elastic',
		openSpeed : 500,
		openEasing : 'easeInCubic'
	});
});
</script>
<script>
$.validate();
</script>
