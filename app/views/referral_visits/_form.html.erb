<%= simple_form_for(@referral_visit) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :referrer %>
    <%= f.input :ref_session %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
